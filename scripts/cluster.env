# cluster.env

MASTER_HOST=group16-master-test
MASTER_IP=192.168.5.54            # private IP of the master VM

# Set 1..3 worker hostnames and IPs you actually have
WORKER_HOSTS=("de1-worker-1" "de1-worker-2" "de1-worker-3")
WORKER_IPS=("192.168.5.188" "192.168.5.225" "192.168.5.249")

# How many workers to start by default
DEFAULT_WORKER_COUNT=3

# Versions/paths
JAVA_PKG="openjdk-17-jdk"
HADOOP_VERSION="3.4.1"
SPARK_VERSION="3.5.0"

# Data dirs
HDFS_NAME_DIR="/data/hdfs/namenode"
HDFS_DATA_DIR="/data/hdfs/datanode"

# HDFS/Spark settings
DFS_REPLICATION=2
SPARK_WORKER_CORES=2
SPARK_WORKER_MEMORY="4g"
SPARK_DRIVER_MEMORY="4g"
SPARK_EXECUTOR_MEMORY="4g"

# Security/SSH
SSH_OPTS="-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
REMOTE_USER="ubuntu"

# Derived paths (no edit)
HADOOP_HOME=/home/ubuntu/hadoop-3.4.1
SPARK_HOME=/home/ubuntu/spark
SPARK_MASTER_URL="spark://${MASTER_IP}:7077"
HDFS_NN_RPC="hdfs://${MASTER_IP}:9000"
``